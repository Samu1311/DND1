@code {
    [Parameter] public EventCallback OnClose { get; set; }
    [Parameter] public EventCallback OnUpgrade { get; set; }
}

<div class="modal-overlay">
    <div class="modal">
        <h2>Upgrade to Premium</h2>
        <p>Unlock full access to all features, including Medical Image Analysis.</p>
        <table>
            <thead>
                <tr>
                    <th>Feature</th>
                    <th>Basic</th>
                    <th>Premium</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Mole Tracking</td>
                    <td>✔️</td>
                    <td>✔️</td>
                </tr>
                <tr>
                    <td>Health Overview</td>
                    <td>✔️</td>
                    <td>✔️</td>
                </tr>
                <tr>
                    <td>Medical Image Analysis</td>
                    <td>❌</td>
                    <td>✔️</td>
                </tr>
            </tbody>
        </table>
        <p><strong>Price:</strong> $5/month or $50/year</p>
        <div class="modal-buttons">
            <button class="btn btn-primary" @onclick="OnUpgrade">Become Premium</button>
            <button class="btn btn-secondary" @onclick="OnClose">Cancel</button>
        </div>
    </div>
</div>

<style>
.modal-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1000;
}

.modal {
    background-color: white;
    border-radius: 12px;
    padding: 20px;
    width: 90%; /* Default width for smaller screens */
    max-width: 500px; /* Cap width for larger screens */
    max-height: 80%; /* Limit height */
    overflow-y: auto; /* Scroll if content overflows */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
    text-align: center;
    display: block; /* Ensures visibility */
    position: relative;
}

/* Table Styling */
table {
    width: 100%;
    border-collapse: collapse;
    margin: 15px 0;
    font-size: 0.9rem; /* Slightly smaller font for compactness */
}

table th,
table td {
    border: 1px solid #ccc;
    padding: 8px; /* Reduced padding */
    text-align: center;
}

table th {
    background-color: #f2f2f2;
    font-weight: bold;
}

table td {
    vertical-align: middle;
}

/* Buttons */
.modal-buttons {
    margin-top: 20px;
    display: flex;
    justify-content: space-evenly; /* Space buttons evenly */
}

.btn {
    padding: 10px 18px;
    border: none;
    border-radius: 5px;
    font-size: 0.9rem;
    cursor: pointer;
}

.btn-primary {
    background-color: #219ebc;
    color: white;
    transition: background-color 0.3s ease, transform 0.2s ease;
}

.btn-primary:hover {
    background-color: #005a70;
    transform: translateY(-3px); /* Subtle hover lift */
}

.btn-secondary {
    background-color: #bf0000;
    color: white;
    transition: background-color 0.3s ease, transform 0.2s ease;
}

.btn-secondary:hover {
    background-color: #4c0000;
    transform: translateY(-3px); /* Subtle hover lift */
}

</style>
